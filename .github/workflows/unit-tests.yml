name: Unit Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: tester
      run: cc -Wall -Werror -Wextra ft_printf.c ft_printf_utils.c tester.c && ./a.out
